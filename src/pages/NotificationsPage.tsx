import { PlaceholderPage } from "../components/PlaceholderPage";

export function NotificationsPage() {
  return (
    <PlaceholderPage
      title="Notifications"
      message="Notifications are planned, but the backend endpoint is not implemented yet."
    />
  );
}
